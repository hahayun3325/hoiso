# Original-crop Gate-B/D0 query and semantic-objective transition

The primary semantic queries now use the original standardized crop available at ingestion. The existing clean-canonical responses are preserved as an assisted foreground ablation. The compiler maps names to exact current MANO and Gate-A geometry; the adapter selects regions, loss terms, and trainable groups while numerical weights remain globally frozen. No optimizer is authorized by this document.
