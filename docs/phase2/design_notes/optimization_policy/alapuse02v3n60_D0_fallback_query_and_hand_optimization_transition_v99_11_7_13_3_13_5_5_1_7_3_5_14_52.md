# D0/D1 fallback-owner decision and optimization transition

Timestamp: 2026-08-14T22:47:40.217006-05:00

The prior owner scan found candidates but proved no historical owner. The current branch explicitly adopts the reviewed professor-toolkit D0 and D1 templates as new hash-bound owners without changing the historical review.

A query uses the evidence required by its role. D0 in this occluded case needs the exact crop, an indexed finger overlay, labeled Gate-A lid/base regions, and Gate-B semantics or an explicit re-query-all fallback. D1 requires final geometry evidence and cannot use the initial crop alone. CSV is an index of reviewed JSON responses; the runtime must receive its path through configuration rather than source edits.

The next order is D0 query and geometry compilation, D0/valid-z-order adapter CPU tests, technical zero, bounded hand refinement, object audit with conditional object updates, bounded joint refinement, and independent D1. No optimizer is authorized by this document.
