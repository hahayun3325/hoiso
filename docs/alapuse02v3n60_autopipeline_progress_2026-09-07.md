# TraceHOI automatic-pipeline progress for `alapuse02v3n60`

Status date: **2026-09-07**. Latest verified worksheet step: **0CO**. Next proposed step: **0CP**, with no execution result returned yet.

The saved-case automatic review command is working. A fresh accepted-foundation-to-review chain has working individual stages and a verified configuration factory; executing that chain through one entry is the next milestone. An accepted refined reconstruction and raw-image-to-final automation remain incomplete.

## What “working” means here

| Milestone | Evidence-backed status |
|---|---|
| Reopen the saved case through one command | **Verified in 0CN.** Four completed-stage resume interfaces run in order, then one case review result is exported. Reusing that case result starts zero child stages. |
| Produce Gate A, Frame I/Gate C and D0 separately from accepted inputs | **Verified in 0CJ–0CL.** Fresh outputs and completed-result reuse were tested. Frame I uses an already accepted hand seed. |
| Connect new D0 to the retained hand/object and J0 review | **Verified in 0CM.** Exact geometry/contract equivalence permits reuse of the saved checkpoint and rejected-trial evidence. |
| Generate isolated stage configurations from preceding receipts | **Verified in 0CO.** One unused Gate-A namespace passed plan-only preflight; three downstream configuration bindings were rehearsed using saved results. No producers ran in 0CO. |
| Execute the fresh producer chain through one entry | **Proposed in 0CP; unverified.** New Gate A → Frame I/Gate C → D0 → retained checkpoint review, followed by completed-case reuse. |
| Accept improved J0 geometry and finish downstream output | **Open.** All three tested bounded J0 proposals remain rejected; D1/F0 closure is unresolved. |
| Raw image → accepted final reconstruction | **Open.** The current route starts from an accepted foundation and an accepted hand seed. |

The accepted controller remains `ready`, `next_index=7`, after `gate_a, frame_i, gate_c, d0, h0, h1, o0`. H0 is a reviewed no-op, and H1/O0 are zero-update identity results. Successful review export does not advance that controller or accept J0.

## Recent changes and observations

- **0CJ:** restored the complete historical PartField runtime in the new launcher. One real Gate-A GPU producer call succeeded; completed reuse made zero producer calls. Whole-object, lid and base hashes matched the earlier accepted Gate-A geometry.
- **0CK:** connected those parts to the accepted hand, applied the owned Frame-I transform, and ran zero-update Gate C. All four published meshes matched the accepted references; reuse made zero calls.
- **0CL:** ran the canonical D0 compiler and full validator once each against those file owners. Region arrays and the semantic contract were preserved, with only declared path/hash rebinding. Reuse made zero calls; `authorizes_optimizer=false`.
- **0CM:** proved equivalence to retained H0/H1/O0 and linked the saved J0 rejection review. One checkpoint-review resume call, then zero calls on bridge reuse. Measurements are explicitly inherited.
- **0CN:** consolidated four completed segment interfaces into one case-review entry. First entry: four segment-resume calls. Entry reuse: zero. All 18 orchestration tests passed in returned server evidence.
- **0CO:** prepared reusable stage-owner builders, copied the four launcher-local code files together, preserved the runtime/foundation bindings, and verified three downstream configuration rehearsals plus one Gate-A plan. All 16 factory tests passed in returned server evidence.

The integration work makes the existing result reproducible and inspectable. It has not improved the retained hand/object geometry. In particular, the saved hand/object IoU is approximately **0.3642 / 0.8288**, with **2,888 of 4,542** observed hand pixels uncovered. Those measurements belong to the saved J0 evidence, not a new 0CN/0CO rendering.

## Why Gate A needed attention after earlier integration

Earlier successful inference depended on four ordered, run-owned dependency directories. A new driver that checked package metadata or `--help` did not reproduce the full lazy PartField model import in its child process. The resulting missing-Lightning/import failures were execution-environment handoff problems, not evidence that the earlier Gate-A result had never worked.

History 05 records the successful setup; the 0CJ audit and runtime-bound execution reproduced it. The four directories, in order, are:

1. `/home/fredcui/foho_phase0/phase2_gateA_part_recon/cases/alapuse02v3n60_auto_v2/tracehoi_autopipeline_0_0_2_20260829T150751Z/runtime/partfield_torch_scatter_v3`
2. `/home/fredcui/foho_phase0/phase2_gateA_part_recon/cases/alapuse02v3n60_auto_v2/tracehoi_autopipeline_0_0_2_20260829T150751Z/runtime/partfield_direct_dependencies_v2`
3. `/home/fredcui/foho_phase0/phase2_gateA_part_recon/cases/alapuse02v3n60_auto_v2/tracehoi_autopipeline_0_0_2_20260829T150751Z/runtime/partfield_boto3_overlay`
4. `/home/fredcui/foho_phase0/phase2_gateA_part_recon/cases/alapuse02v3n60_auto_v2/tracehoi_autopipeline_0_0_1_20260829T140828Z/runtime/partfield_python_overlay`

The ordered search path then includes the checkout's `src` directory and the PartField checkout. The recorded interpreter is **Anaconda `foho`**, Python 3.10.20, with PyTorch 2.5.0+cu124 and Lightning 2.2.5 in the reviewed runtime. The audit passed 18 probes. Three declared-dependency findings involving fsspec, packaging and aiohttp remain recorded; the scoped local-file GPU execution does not certify every optional package feature.

Preserve the installed runtime directories, the runtime lock and the external PartField feature files. Copied review ZIPs preserve evidence; they are not replacements for those dependencies. Bind the ordered environment to every child invocation. Keep immutable source inventories separate from generated `lightning_logs` and `exp_results` outputs. Reuse completed Gate A for downstream-only work.

## Current blockers and next execution

The orchestration gap is now a fresh execution across new file owners. The proposed **0CP** entry will create its own run directory, use the verified 0CO factory, and:

1. Prepare a new Gate-A owner with the tested runtime, check its unused output/feature namespace, then run Gate A once.
2. Generate the Frame-I/Gate-C configuration from that new completion, then execute it using the accepted hand seed.
3. Generate D0 configuration from the new Frame-I/Gate-C completion, then compile and validate it.
4. Generate the retained-review bridge from the new D0 completion and export one case review result.
5. Verify completed-case reuse with zero child-stage invocations, including the external feature hashes.

0CO's saved-receipt rehearsal configurations must not be executed as the fresh chain. 0CP uses a separate directory and regenerates each downstream binding after the preceding producer finishes. A changed geometry hash stops reuse of the historical semantic/contact maps. A failed or partial attempt is preserved; dependent stages are skipped and it is not automatically retried.

The remaining reconstruction work is separate:

- The rigid adapter does not implement the selected-finger articulation specified by D0.
- Native units are not calibrated to millimetres. Unsigned proximity alone does not establish physical contact or nonpenetration.
- The three bounded J0 proposals failed their filters. Keep `J0_accepted=false`, `authorizes_optimizer=false` and `pipeline_complete=false` until new evidence supports a different decision.
- Automatic selected-hand production and downstream D1/F0/evaluation closure still need demonstrated integration.

Planning estimate: the saved-case review milestone is achieved. After verified 0CO, budget roughly **1–2 further implementation/execution/review cycles** for the fresh accepted-foundation-to-review demonstration if runtime and geometry remain consistent. The earlier 1–2 focused engineering-week estimate from verified 0CJ remains a planning range, not a timer reset at every worksheet. An accepted refined result is roughly **2–4+ weeks**, with substantial hand/contact uncertainty. Raw-image-to-final completion has no reliable date yet.

## Evidence and reproducibility

This report is based on returned server packages and local byte-level review. It does **not** establish a direct server connection or the server's current Git HEAD. The repository branch inspected for this documentation was `phase-2.1-agile-vlm-upstream-gate` at `81d94e0` before this documentation commit. Newer worksheet drivers live in server diagnostic directories; this document does not claim that they have all been installed in the repository's production entrypoint.

| Package | Bytes | Verified payloads | SHA256 |
|---|---:|---:|---|
| `step_0cj_bound_runtime_trial_v1_review.zip` | 6,746,375 | 57 | `23430d3069130818ea00f6305d496e4a2b590d6d16fe0204ac47d1f9e2c19f86` |
| `step_0ck_gate_a_frame_i_replay_v1_review.zip` | 9,207,388 | 29 | `af3397fb5235cbd7df9a973ebf1019e7412bfd0e921e5caef15278c84155a971` |
| `step_0cl_fresh_d0_compile_v1_review.zip` | 9,357,936 | 32 | `90acab9a766f9a735fa36f570e8b66cce9366b5730a1ad3d187f09f22ea1cf66` |
| `step_0cm_retained_review_bridge_v1_review.zip` | 14,552,609 | 26 | `8c9d70e8cec26ca9d98bb08a368c4db775d8725b926cfc704d63656e5769eb65` |
| `step_0cn_case_review_entry_v1_review.zip` | 18,088,885 | 35 | `c640b4cfb5b24081bc6ef1c262728d96e52ee23c3166bb1c358b3fd1b89ea32c` |
| `step_0co_fresh_owner_factory_v1_review.zip` | 124,884 | 31 | `a1eee94d3dc30311ade9f2f59827cb8fd2ef704c66d46615c60e11342e29a80f` |

0CN's 7 prepared-file records, 18 outer run-file records and 16 case-output records match its copied files. Its 215 case-input bindings agree with the configuration, and its run/resume records agree. 0CO's 24 artifact records match its copied files; its completion records 234 pinned inputs. The returned factory source matches the prepared source exactly. CRC and manifest payload checks support package integrity; they do not by themselves prove reconstruction quality.

Key unchanged bindings:

- Accepted State: `a04c07db88b41c5aab4fe1420c8fcf2aea78899176cbc8af9117ac51d03796e5`.
- Retained hand: `bc0b7b869808049f2c256c3b756e792f76dce08ee28f6c45d5221913cb49f068`.
- Retained whole object: `25e8c00e9069f2abd22b26f3a1f37c40be333d4a83f2218dc641b78fec9748bb`.
- 0CN case terminal: `5b904330f071e9f91ded5d1b49b64f45e453fbebf4bc3fe1de4f9d833273b2ca`.
- 0CO preparation plan: `a73997d860477b4544f12c4f45addeac6d2c560cf76350a8ec92bd7f3761ba2b`.

## Path map and continuation rule

- Server checkout: `/home/fredcui/Projects/FollowMyHold`.
- Server interpreter: `/home/fredcui/anaconda3/envs/foho/bin/python`.
- Case root: `/home/fredcui/foho_phase0/phase2_gateA_part_recon/cases/alapuse02v3n60_auto_v2`.
- Diagnostics root: `/home/fredcui/foho_phase0/phase2_gateA_part_recon/cases/alapuse02v3n60_auto_v2/tracehoi_autopipeline_0_0_5_20260831T131948Z/diagnostics`.
- Saved-case entry, relative to diagnostics: `step_0cn_case_review_entry_v1/case_review.py`, with `case_review_config.json` in the same directory. Its `status`, `run` and `resume` modes operate on the completed saved case; `run` is not a force-rerun option.
- Verified factory: `step_0co_fresh_owner_factory_v1/artifacts/owner_factory.py` and `factory_spec.json`.
- Proposed next owner: `step_0cp_fresh_case_chain_v1` — no returned execution evidence as of this report.
- Current worksheet: **0.0.5.3 alapuse02v3n60 tracehoi autopipeline**, in the Obsidian vault. It contains self-contained server commands, expected outputs and failure recovery.
- Closed worksheet: **0.0.5.2**, through verified 0CI. Closure reflects document length, not pipeline completion.
- Current Mac transfers: `/Users/fredcui/Downloads/wacv-2027-our-kit/step_0az_saved_input_review`.
- Historical review archives: `/Volumes/disk/TraceHOI/coding_evidence/alapuse02v3n60`.

Run models on the RTX 4090 server; use the Mac for documentation, transfers and lightweight inspection. Preserve failed diagnostics and keep terminal errors visible. Advance only after verifying the returned result, and update this dated status or add a clearly dated successor rather than silently reinterpreting historical acceptance.

## Related repository history

- [Frame-I hand-specific pose initialization](alapuse02v3n60_frame_i_pose_initialization.md): explains why hand placement has its own owner and why coarse lid proximity is a scoped initialization result.
- [Earlier single-command status and integration plan](phase2/design_notes/automation/tracehoi_single_command_completion_status_and_integration_plan_v99_11_7_13_3_13_5_5_1_7_3_5_14_112_59.md): historical front-half and post-Q2 integration boundary. Its “current” status and time estimate describe that earlier lineage.
- [Earlier six-of-seven foundation and selected-hand boundary](phase2/design_notes/automation/alapuse02v3n60_foundation_six_of_seven_and_selected_hand_boundary_v99_11_7_13_3_13_5_5_1_7_3_5_14_112_77.md): historical foundation-stage ownership and the unresolved hand-selection boundary at that time.

This report supersedes those documents only for the latest progress status. Their algorithm details and lineage-specific evidence remain historical references.
