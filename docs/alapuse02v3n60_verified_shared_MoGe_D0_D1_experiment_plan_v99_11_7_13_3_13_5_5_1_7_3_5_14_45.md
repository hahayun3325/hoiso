# alapuse02v3n60: verified shared-MoGe, D0/D1, and experiment plan

## Current result

The finite-guarded 20-update hand probe completed, but no checkpoint passed the
zero-state gates. Step 5 is retained only as a selected-finger initializer.
The step-6 z-order term used incomplete object depth support and must be
reimplemented with valid dense depth before further optimization.

## Coordinate routes

- Trusted Gate-A object: `G -> I` by the accepted direct object-to-MoGe fit.
- Selected HaMeR hand: `H -> Uhoi -> I` through the accepted clean carrier and
  accepted `T_Uhoi_to_I`.
- Both endpoints meet in the exact same hash-owned MoGe frame `I`; identical
  intermediate routes are not required.

## Gates

- Auto-v2 blocks/reopens failed upstream evidence.
- Gate A owns articulated object parts and topology.
- Gate B proposes coarse hand/part/finger contact semantics.
- Gate C closes coordinate and initial-state ownership in MoGe.
- Gate D0 compiles Gate-B semantics into exact MANO IDs, Gate-A face patches,
  forbidden regions, z-order, confidence, and contact-gap rules.
- Gate D1 independently validates localized contact, collision, image/depth
  consistency, and invariants after refinement.

## Next experiment

1. Complete the D0 mapping without inventing uncertain fingers or faces.
2. Replace sparse/sentinel z-order supervision with dense valid object depth.
3. Run a zero-update live replay of the exact initializer.
4. Run at most five selected-finger articulation updates with object, hand
   global transform, scale, shape, and nonselected joints frozen.
5. Audit the object zero state; skip object updates if seed 2026 remains valid.
6. Run a short D0-guided joint trust region only after separate passes.
7. Run independent D1 and retain rollback at every stage.

No GPU or Gate-D budget is authorized by this note.
