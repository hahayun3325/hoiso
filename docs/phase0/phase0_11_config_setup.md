# Phase 0.11 — Phase 0 Smoke-Test Config

## Goal

Create a local config for the first FollowMyHold smoke test.

## Local config

The local config is:

`configs/pipeline.phase0.env`

This file contains real local paths and API keys, so it must not be committed.

## Safe template

A sanitized template is stored as:
`configs/pipeline.phase0.template.env`

## Output folder

The first smoke-test output folder is:
`~/foho_phase0/runs/smoke_001`

## Next step

After this phase, run import/help checks and then launch the first smoke test.  
