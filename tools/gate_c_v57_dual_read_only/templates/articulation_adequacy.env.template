# Copy to the case audit root and fill exact immutable paths.
export REPO=/home/fredcui/Projects/FollowMyHold
export DATA=/home/fredcui/foho_phase0
export CASE_ROOT="$DATA/phase2_gateA_part_recon/cases/alapuse02v3n60_auto_v2"
export TOOL_ROOT="$REPO/tools/gate_c_articulation_adequacy_v1"
export AUDIT_ROOT="$CASE_ROOT/gate_c1_5_read_only_articulation_adequacy_v1"

export PROBE_CONFIG="$AUDIT_ROOT/config/probe_config.json"
export PARAMETER_MANIFEST="$AUDIT_ROOT/manifests/parameter_manifest.csv"
export SOURCE_ADAPTER="$AUDIT_ROOT/adapter/source_bound_adapter.py"
export THRESHOLDS="$AUDIT_ROOT/config/articulation_adequacy_thresholds.json"

# Exact registered target-raster arrays.
export TARGET_KEYPOINTS=/ABS/PATH/to/frozen_target_2d_keypoints.npy
export EXPECTED_ZERO_KEYPOINTS=/ABS/PATH/to/validated_zero_update_projection.npy
export TARGET_CONFIDENCE=''  # Optional N-vector .npy/.json; empty means all 1.

# Use the exact denominator from the registered Branch-E normalized metrics.
# Do not silently recompute it from a different crop or panel image.
export NORMALIZATION_SCALE_PX=0.0
export BRANCH_E_TRANSLATION_RADIUS_M=0.0  # exact registered L2 trust radius
